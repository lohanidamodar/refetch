{
  "name": "digest-function",
  "version": "1.0.0",
  "description": "Daily push digest of top Refetch stories to the digest topic",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"no tests\" && exit 0"
  },
  "keywords": ["appwrite", "function", "nodejs", "messaging", "push", "cron"],
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "node-appwrite": "^24.1.0"
  }
}
